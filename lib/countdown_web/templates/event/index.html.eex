<h1>Listing Events</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Due</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for event <- @events do %>
    <tr>
      <td><%= event.title %></td>
      <td><%= event.due %></td>

      <td>
        <%= link "Show", to: Routes.event_path(@conn, :show, event) %> &nbsp
        <%= link "Edit", to: Routes.event_path(@conn, :edit, event) %> &nbsp
        <%= link "Delete", to: Routes.event_path(@conn, :delete, event), method: :delete, data: [confirm: "Are you sure?"] %>
      </td>
    </tr>
<% end %>
  </tbody>&nbsp
</table>

<span><%= link "New Event", to: Routes.event_path(@conn, :new) %></span>
